 // when we use new operator for dynamic memory allocation, we will need a pointer
 // because when we use new operator it allocates memory dynamically 
 // so when the memory allocation occurs so it return that address
 // which we stores at the pointer.

  